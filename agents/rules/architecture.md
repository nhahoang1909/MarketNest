# Architecture Rules (moved)

The canonical agent guidelines have been consolidated. See `agents/GUIDELINES.md` for the current agent-facing rules and pointers to authoritative `docs/` pages.

Original/extended rules have been preserved under `agents/rules/archive/architecture.md`.
