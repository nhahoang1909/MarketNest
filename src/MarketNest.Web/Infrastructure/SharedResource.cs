namespace MarketNest.Web;

/// <summary>
///     Marker class for shared localization resources (SharedResource.{culture}.resx).
///     Must live in the root namespace (MarketNest.Web) so that ASP.NET Core localization
///     resolves resource files at Resources/SharedResource.{culture}.resx.
/// </summary>
public class SharedResource;
