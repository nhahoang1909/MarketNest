@page
@model IndexModel
@{
  ViewData["Title"] = "Home";
}

<div class="text-center py-20">
  <h1 class="text-4xl font-bold text-gray-900 mb-4">Welcome to MarketNest</h1>
  <p class="text-lg text-gray-600 mb-8">A multi-vendor marketplace — discover unique products from independent
    sellers.</p>
  <a href="/search"
     class="inline-block bg-brand-500 text-white px-8 py-3 rounded-lg font-semibold hover:bg-brand-600 transition">
    Browse Products
  </a>
</div>
