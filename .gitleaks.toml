# Gitleaks configuration for MarketNest
title = "MarketNest gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Global allowlist"
paths = [
    '''\.env\.example$''',
    '''docs/.*\.md$''',
]
