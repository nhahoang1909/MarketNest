# ── .NET ──────────────────────────────────────────────────
bin/
obj/
publish/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
*.nupkg
*.snupkg

# ── IDE ───────────────────────────────────────────────────
.idea/
*.swp
*~
.vscode/settings.json

# ── Node / Frontend ──────────────────────────────────────
node_modules/
src/MarketNest.Web/wwwroot/css/site.css

# ── Secrets & Environment ─────────────────────────────────
.env
.env.local
.env.production
!.env.example
appsettings.Production.json
appsettings.Secrets.json
**/user-secrets/

# ── Docker ────────────────────────────────────────────────
infra/nginx/certs/*.pem
infra/nginx/certs/*.key

# ── OS ────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Logs ──────────────────────────────────────────────────
logs/
*.log

# ── Test Results ──────────────────────────────────────────
TestResults/
*.trx
coverage/

# ── Claude Code ─────────────────────────────────────────────
# Claude Code creates git worktrees here for parallel sessions.
# Each worktree contains a .git file, which GitHub Actions'
# `git submodule foreach` misidentifies as an unregistered submodule,
# causing fatal post-job cleanup failures. Always ignore these.
.claude/worktrees/
.claude/settings.local.json

